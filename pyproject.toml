[tool.ruff]
line-length = 130

[tool.ruff.lint]
# Enable Pyflakes (F), pycodestyle (E, W), and pydocstyle (D)
select = ["F", "E", "W", "D"]

[tool.ruff.lint.pydocstyle]
# Use the Google convention for docstrings
convention = "google"

[tool.pytest.ini_options]
addopts = "--ruff"
